{
  "name": "cobblemon-kinetics-workspace",
  "version": "0.1.0-alpha.1",
  "private": true,
  "description": "Web studio, data contracts, and deterministic tooling for Create: Cobblemon Kinetics.",
  "license": "MPL-2.0",
  "packageManager": "pnpm@11.19.0",
  "engines": {
    "node": ">=24.0.0 <25",
    "pnpm": ">=11.19.0 <12"
  },
  "scripts": {
    "guard": "node scripts/check-repository-hygiene.mjs",
    "format": "prettier --write apps/web packages/domain/src packages/domain/test packages/domain/scripts packages/data-tools/src packages/data-tools/test scripts .github/workflows/web.yml package.json pnpm-workspace.yaml prettier.config.mjs tsconfig.base.json docs/WEBSITE.md --ignore-unknown --ignore-path .prettierignore",
    "format:check": "prettier --check apps/web packages/domain/src packages/domain/test packages/domain/scripts packages/data-tools/src packages/data-tools/test scripts .github/workflows/web.yml package.json pnpm-workspace.yaml prettier.config.mjs tsconfig.base.json docs/WEBSITE.md --ignore-unknown --ignore-path .prettierignore",
    "build": "pnpm -r --if-present build",
    "build:web": "pnpm --filter @cobblemon-kinetics/web build",
    "dev": "pnpm --filter @cobblemon-kinetics/web dev",
    "lint": "pnpm -r --if-present lint",
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm test:scripts && pnpm -r --if-present test",
    "test:scripts": "node --test scripts/*.test.mjs",
    "test:web": "pnpm --filter @cobblemon-kinetics/web test",
    "e2e": "pnpm --filter @cobblemon-kinetics/web e2e",
    "data:import": "pnpm --filter @cobblemon-kinetics/data-tools import:workbook",
    "data:apply": "pnpm --filter @cobblemon-kinetics/data-tools apply:publication",
    "data:verify": "pnpm --filter @cobblemon-kinetics/data-tools verify:published",
    "db:start": "supabase start",
    "db:stop": "supabase stop",
    "db:reset": "supabase db reset",
    "db:test": "supabase test db",
    "db:types": "supabase gen types typescript --local --schema public | node scripts/normalize-generated-text.mjs > apps/web/src/types/database.generated.ts",
    "access:grant": "pnpm --filter @cobblemon-kinetics/data-tools access:grant",
    "backup:create": "node scripts/create-prototype-backup.mjs"
  },
  "devDependencies": {
    "prettier": "3.9.6",
    "supabase": "2.114.0"
  }
}
